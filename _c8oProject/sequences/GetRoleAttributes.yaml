accessibility: Private
comment: 'Get attributes for a role. Parameter: role is the role name. The sequence reads the deterministic attribute document sha256("roleAttributes:" + role), whose type is c8oRoleAttributes. The response is the FullSync document returned by GetDocument and contains couchdb_output.attributes as the JSON object previously written by SetRoleAttributes.'
↓BuildDocId [steps.SimpleStep-1781090099970]: 
  expression: |
    if (role == undefined || ('' + role).trim().length < 1) { throw 'The "role" parameter must be defined and not empty'; }
    role = ('' + role).trim().toLowerCase();
    var _use_docid = org.apache.commons.codec.digest.DigestUtils.sha256Hex('roleAttributes:' + role);
↓Call_GetRoleAttributes [steps.TransactionStep-1781090099972]: 
  output: true
  sourceTransaction: lib_FullSyncGrp.c8ofullsyncgrp.GetRoleAttributes
  ↓_use_docid [variables.StepVariable-1781090099974]: 
↓role [variables.RequestableVariable-1781090099976]: 
  comment: Role name whose attributes document is read. The read document id is sha256("roleAttributes:" + role).
  description: Role name whose attributes document is read.